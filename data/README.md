# Data Directory

This folder stores raw and processed datasets used for model training and evaluation.

## Structure
- `raw/`: Unmodified source data fetched from external providers.
- `processed/`: Cleaned and feature-engineered datasets ready for modeling.

Note: Large files and proprietary datasets should not be committed to source control. Update `.gitignore` rules as needed.
